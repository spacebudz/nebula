{
  "tasks": {
    "build": "deno run -A build.ts && deno run -A https://deno.land/x/lucid@0.10.6/blueprint.ts",
    "build:contract": "cd src/nebula && aiken build",
    "test": "deno test --allow-env --allow-read --allow-net tests",
    "test:contract": "cd src/nebula && aiken check"
  },
  "lint": {
    "files": {
      "include": ["src/"],
      "exclude": ["src/ghc/", "src/onchain.hs"]
    }
  }
}
